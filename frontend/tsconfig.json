{
  "compilerOptions": {
    "lib": ["DOM", "DOM.Iterable", "ES2022"],
    "types": ["node", "vite/client"], // Kamu bisa menambahkan 'react-splide' jika ada tipe eksternal atau deklarasi manual
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "rootDirs": [".", "./.react-router/types"], // Pastikan folder ini ada jika diperlukan
    "baseUrl": ".", // Basis untuk resolusi modul relatif
    "paths": {
      "~/*": ["./app/*"] // Alias untuk folder 'app', sesuaikan jika kamu menggunakan alias lain
    },
    "esModuleInterop": true,
    "verbatimModuleSyntax": true,
    "noEmit": true, // Tidak mengeluarkan file js saat kompilasi
    "resolveJsonModule": true,
    "skipLibCheck": true, // Mengabaikan pengecekan tipe untuk library eksternal
    "strict": true // Aktifkan semua pemeriksaan ketat untuk kualitas kode yang lebih baik
  },
  "include": [
    "**/*", // Sertakan semua file dalam proyek
    "**/.server/**/*",
    "**/.client/**/*",
    ".react-router/types/**/*" // Sertakan folder tipe untuk React Router jika diperlukan
  ]
}
